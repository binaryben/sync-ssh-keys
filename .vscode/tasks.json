{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Compile binary",
			"command": "nimble",
			"args": ["build", "-d:ssl"],
			// "options": { "cwd": "${workspaceRoot}" },
			"type": "shell",
			"group": {
				"kind": "build",
				"isDefault": true
			}
		}
	]
}